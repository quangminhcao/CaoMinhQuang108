@model IEnumerable<CaoMinhQuang108.Models.CMQ0108>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CMQName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CMQGender)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CMQName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CMQGender)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CMQId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CMQId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CMQId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
